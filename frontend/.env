VITE_API_URL=http://localhost:8080
# Publishable keys are safe to expose client-side by design. This is a placeholder --
# the backend's stripe.secret.key is a dummy test value too, so real card confirmation
# won't succeed locally until both are replaced with a matching real Stripe test-mode pair.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_placeholder
